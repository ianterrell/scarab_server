<h2>Authors</h2>

<% if @authors.empty? %>
  <p>
    No authors.
  </p>
<% else %>
  <table>
    <tr>
      <th>Name</th>
      <th>Issue Count</th>
    </tr>
    <% @authors.each do |author| %>
      <tr>
        <td><%= link_to author.name, author %></td>
        <td><%= author.issues.count %></td>
      </tr>
    <% end %>
  </table>
  <%= will_paginate @authors %>
<% end %>


<p>
  Authors are considered "published" once they are referenced in an issue.  After that point, all content is seen by
  the public -- so double check spelling, proofread bios, and make sure all links are accurate before then!
</p>

<%= link_to "New Author", new_author_path %>