<h2>Works</h2>

<% if @works.empty? %>
  <p>
    No works.
  </p>
<% else %>
  <table>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th>Issue</th>
    </tr>
    <% @works.each do |work| %>
      <tr>
        <td><%= link_to work.title, work %></td>
        <td><%= link_to work.author.name, work.author %></td>
        <td><%= link_to work.issue.title, work.issue %></td>
      </tr>
    <% end %>
  </table>
  <%= will_paginate @works %>
<% end %>


<p>
  Works are considered "published" once they are referenced in an issue.  After that point, all content is seen by
  the public -- so proofread carefully!
</p>

<%= link_to "New Work", new_work_path %>